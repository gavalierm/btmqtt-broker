{
  "name": "btmqtt-broker",
  "version": "1.0.0",
  "description": "Main function is provide MQTT service for btmqtt-esp to send and receive bluetooth payload to blackmagic camera via MQTT.",
  "main": "app.js",
  "scripts": {
    "run": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aedes": "^0.50.1",
    "atem-connection": "^3.4.0",
    "mqtt": "^5.3.4",
    "websocket-stream": "^5.5.2"
  }
}
